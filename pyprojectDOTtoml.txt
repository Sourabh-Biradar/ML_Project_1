[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ML_Project_1"
version = "0.1.0"
description = "An ML project"
authors = [
    { name = "Sourabh Biradar" }
]
readme = "README.md"
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "numpy",
    "seaborn",
    "matplotlib"
]